import {
  computed,
  createElementBlock,
  defineComponent,
  inject,
  mergeProps,
  nextTick,
  onMounted,
  onUnmounted,
  openBlock,
  provide,
  ref,
  renderSlot,
  toValue,
  unref,
  watch,
  watchEffect
} from "./chunk-EGBKDYOK.js";

// node_modules/esm-env/true.js
var true_default = true;

// node_modules/number-flow/dist/ssr-DVNwbNNo.mjs
var b = String.raw;
var m = String.raw;
var k = true_default && (() => {
  try {
    document.createElement("div").animate({ opacity: 0 }, { easing: "linear(0, 1)" });
  } catch {
    return false;
  }
  return true;
})();
var S = true_default && typeof CSS < "u" && CSS.supports && CSS.supports("line-height", "mod(1,1)");
var _ = true_default && typeof matchMedia < "u" ? matchMedia("(prefers-reduced-motion: reduce)") : null;
var g = "--_number-flow-d-opacity";
var u = "--_number-flow-d-width";
var c = "--_number-flow-dx";
var h = "--_number-flow-d";
var x = (() => {
  try {
    return CSS.registerProperty({
      name: g,
      syntax: "<number>",
      inherits: false,
      initialValue: "0"
    }), CSS.registerProperty({
      name: c,
      syntax: "<length>",
      inherits: true,
      initialValue: "0px"
    }), CSS.registerProperty({
      name: u,
      syntax: "<number>",
      inherits: false,
      initialValue: "0"
    }), CSS.registerProperty({
      name: h,
      syntax: "<number>",
      inherits: true,
      initialValue: "0"
    }), true;
  } catch {
    return false;
  }
})();
var d = "var(--number-flow-char-height, 1em)";
var s = "round(nearest, calc(var(--number-flow-mask-height, 0.25em) / 2), 1px)";
var e = `calc(${s} * 2)`;
var p = "var(--number-flow-mask-width, 0.5em)";
var n = `calc(${p} / var(--scale-x))`;
var r = "#000 0, transparent 71%";
var M = m`:host{display:inline-block;direction:ltr;white-space:nowrap;isolation:isolate;line-height:${d} !important}.number,.number__inner{display:inline-block;transform-origin:left top}:host([data-will-change]) :is(.number,.number__inner,.section,.digit,.digit__num,.symbol){will-change:transform}.number{--scale-x:calc(1 + var(${u}) / var(--width));transform:translateX(var(${c})) scaleX(var(--scale-x));margin:0 calc(-1 * ${p});position:relative;-webkit-mask-image:linear-gradient(to right,transparent 0,#000 ${n},#000 calc(100% - ${n}),transparent ),linear-gradient(to bottom,transparent 0,#000 ${e},#000 calc(100% - ${e}),transparent 100% ),radial-gradient(at bottom right,${r}),radial-gradient(at bottom left,${r}),radial-gradient(at top left,${r}),radial-gradient(at top right,${r});-webkit-mask-size:100% calc(100% - ${e} * 2),calc(100% - ${n} * 2) 100%,${n} ${e},${n} ${e},${n} ${e},${n} ${e};-webkit-mask-position:center,center,top left,top right,bottom right,bottom left;-webkit-mask-repeat:no-repeat}.number__inner{padding:${s} ${p};transform:scaleX(calc(1 / var(--scale-x))) translateX(calc(-1 * var(${c})))}:host > :not(.number){z-index:5}.section,.symbol{display:inline-block;position:relative;isolation:isolate}.section::after{content:'\200b';display:inline-block}.section--justify-left{transform-origin:center left}.section--justify-right{transform-origin:center right}.section > [inert],.symbol > [inert]{margin:0 !important;position:absolute !important;z-index:-1}.digit{display:inline-block;position:relative;--c:var(--current) + var(${h})}.digit__num,.number .section::after{padding:${s} 0}.digit__num{display:inline-block;--offset-raw:mod(var(--length) + var(--n) - mod(var(--c),var(--length)),var(--length));--offset:calc( var(--offset-raw) - var(--length) * round(down,var(--offset-raw) / (var(--length) / 2),1) );--y:clamp(-100%,var(--offset) * 100%,100%);transform:translateY(var(--y))}.digit__num[inert]{position:absolute;top:0;left:50%;transform:translateX(-50%) translateY(var(--y))}.digit:not(.is-spinning) .digit__num[inert]{display:none}.symbol__value{display:inline-block;mix-blend-mode:plus-lighter;white-space:pre}.section--justify-left .symbol > [inert]{left:0}.section--justify-right .symbol > [inert]{right:0}.animate-presence{opacity:calc(1 + var(${g}))}`;
var V = true_default ? HTMLElement : class {
};
var y = m`:host{display:inline-block;direction:ltr;white-space:nowrap;line-height:${d} !important}span{display:inline-block}:host([data-will-change]) span{will-change:transform}.number,.digit{padding:${s} 0}.symbol{white-space:pre}`;
var $ = (t) => `<span class="${t.type === "integer" || t.type === "fraction" ? "digit" : "symbol"}" part="${t.type === "integer" || t.type === "fraction" ? `digit ${t.type}-digit` : `symbol ${t.type}`}">${t.value}</span>`;
var i = (t, a) => `<span part="${a}">${t.reduce((l2, f3) => l2 + $(f3), "")}</span>`;
var w = (t = "") => m`number-flow${t} > span{font-kerning:none;display:inline-block;line-height:${d} !important;padding:${e} 0}`;
var C = (t, { nonce: a, elementSuffix: l2 } = {}) => (
  // shadowroot="open" non-standard attribute for old Chrome:
  b`<template shadowroot="open" shadowrootmode="open"
			><style${a ? ` nonce="${a}"` : ""}>${y}</style
			><span role="img" aria-label="${t.valueAsString}"
				>${i(t.pre, "left")}<span part="number" class="number"
					>${i(t.integer, "integer")}${i(t.fraction, "fraction")}</span
				>${i(t.post, "right")}</span
			></template
		><style${a ? ` nonce="${a}"` : ""}>${w(l2)}</style
		><span>${t.valueAsString}</span>`
);

// node_modules/number-flow/dist/plugins.mjs
var f = (e2, n2) => e2 == null ? n2 : n2 == null ? e2 : Math.max(e2, n2);
var i2 = /* @__PURE__ */ new WeakMap();
var l = {
  onUpdate(e2, n2, o) {
    if (i2.set(o, void 0), !o.computedTrend)
      return;
    const s3 = n2.integer.concat(n2.fraction).filter((t) => t.type === "integer" || t.type === "fraction"), r2 = e2.integer.concat(e2.fraction).filter((t) => t.type === "integer" || t.type === "fraction"), u2 = s3.find((t) => !r2.find((c3) => c3.pos === t.pos && c3.value === t.value)), a = r2.find((t) => !s3.find((c3) => t.pos === c3.pos && t.value === c3.value));
    i2.set(o, f(u2 == null ? void 0 : u2.pos, a == null ? void 0 : a.pos));
  },
  getDelta(e2, n2, o) {
    const s3 = e2 - n2, r2 = i2.get(o.flow);
    if (!s3 && r2 != null && r2 >= o.pos)
      return o.length * o.flow.computedTrend;
  }
};

// node_modules/number-flow/dist/lite.mjs
var p2 = (n2, t, e2) => {
  const i3 = document.createElement(n2), [s3, o] = Array.isArray(t) ? [void 0, t] : [t, e2];
  return s3 && Object.assign(i3, s3), o == null || o.forEach((a) => i3.appendChild(a)), i3;
};
var V2 = (n2, t) => {
  var e2;
  return t === "left" ? n2.offsetLeft : (((e2 = n2.offsetParent instanceof HTMLElement ? n2.offsetParent : null) == null ? void 0 : e2.offsetWidth) ?? 0) - n2.offsetWidth - n2.offsetLeft;
};
var W = (n2) => n2.offsetWidth > 0 && n2.offsetHeight > 0;
var X = (n2, t) => {
  true_default && !customElements.get(n2) && customElements.define(n2, t);
};
function k2(n2, t, { reverse: e2 = false } = {}) {
  const i3 = n2.length;
  for (let s3 = e2 ? i3 - 1 : 0; e2 ? s3 >= 0 : s3 < i3; e2 ? s3-- : s3++)
    t(n2[s3], s3);
}
function z(n2, t, e2, i3) {
  const s3 = t.formatToParts(n2);
  e2 && s3.unshift({ type: "prefix", value: e2 }), i3 && s3.push({ type: "suffix", value: i3 });
  const o = [], a = [], r2 = [], d2 = [], c3 = {}, f3 = (l2) => `${l2}:${c3[l2] = (c3[l2] ?? -1) + 1}`;
  let u2 = "", m2 = false, g3 = false;
  for (const l2 of s3) {
    u2 += l2.value;
    const h2 = l2.type === "minusSign" || l2.type === "plusSign" ? "sign" : l2.type;
    h2 === "integer" ? (m2 = true, a.push(...l2.value.split("").map((_3) => ({ type: h2, value: parseInt(_3) })))) : h2 === "group" ? a.push({ type: h2, value: l2.value }) : h2 === "decimal" ? (g3 = true, r2.push({ type: h2, value: l2.value, key: f3(h2) })) : h2 === "fraction" ? r2.push(...l2.value.split("").map((_3) => ({
      type: h2,
      value: parseInt(_3),
      key: f3(h2),
      pos: -1 - c3[h2]
    }))) : (m2 || g3 ? d2 : o).push({
      type: h2,
      value: l2.value,
      key: f3(h2)
    });
  }
  const v = [];
  for (let l2 = a.length - 1; l2 >= 0; l2--) {
    const h2 = a[l2];
    v.unshift(h2.type === "integer" ? {
      ...h2,
      key: f3(h2.type),
      pos: c3[h2.type]
    } : {
      ...h2,
      key: f3(h2.type)
    });
  }
  return {
    pre: o,
    integer: v,
    fraction: r2,
    post: d2,
    valueAsString: u2,
    value: typeof n2 == "string" ? parseFloat(n2) : n2
  };
}
var B = S && k && x;
var D = class extends V {
  constructor() {
    super(), this.created = false, this.batched = false;
    const { animated: t, ...e2 } = this.constructor.defaultProps;
    this._animated = this.computedAnimated = t, Object.assign(this, e2);
  }
  get animated() {
    return this._animated;
  }
  set animated(t) {
    var e2;
    this.animated !== t && (this._animated = t, (e2 = this.shadowRoot) == null || e2.getAnimations().forEach((i3) => i3.finish()));
  }
  /**
   * @internal
   */
  set data(t) {
    var r2, d2;
    if (t == null)
      return;
    const { pre: e2, integer: i3, fraction: s3, post: o, value: a } = t;
    if (this.created) {
      const c3 = this._data;
      this._data = t, this.computedTrend = typeof this.trend == "function" ? this.trend(c3.value, a) : this.trend, this.computedAnimated = B && this._animated && (!this.respectMotionPreference || !((r2 = _) != null && r2.matches)) && // https://github.com/barvian/number-flow/issues/9
      W(this) && // https://github.com/barvian/number-flow/issues/165
      this.ownerDocument.visibilityState === "visible", (d2 = this.plugins) == null || d2.forEach((f3) => {
        var u2;
        return (u2 = f3.onUpdate) == null ? void 0 : u2.call(f3, t, c3, this);
      }), this.batched || this.willUpdate(), this._pre.update(e2), this._num.update({ integer: i3, fraction: s3 }), this._post.update(o), this.batched || this.didUpdate();
    } else {
      this._data = t, this.attachShadow({ mode: "open" });
      try {
        this._internals ?? (this._internals = this.attachInternals()), this._internals.role = "img";
      } catch {
      }
      const c3 = document.createElement("style");
      this.nonce && (c3.nonce = this.nonce), c3.textContent = M, this.shadowRoot.appendChild(c3), this._pre = new A(this, e2, {
        justify: "right",
        part: "left"
      }), this.shadowRoot.appendChild(this._pre.el), this._num = new F(this, i3, s3), this.shadowRoot.appendChild(this._num.el), this._post = new A(this, o, {
        justify: "left",
        part: "right"
      }), this.shadowRoot.appendChild(this._post.el), this.created = true;
    }
    try {
      this._internals.ariaLabel = t.valueAsString;
    } catch {
    }
  }
  /**
   * @internal
   */
  willUpdate() {
    this._pre.willUpdate(), this._num.willUpdate(), this._post.willUpdate();
  }
  /**
   * @internal
   */
  didUpdate() {
    if (!this.computedAnimated)
      return;
    this._abortAnimationsFinish ? this._abortAnimationsFinish.abort() : this.dispatchEvent(new Event("animationsstart")), this._pre.didUpdate(), this._num.didUpdate(), this._post.didUpdate();
    const t = new AbortController();
    Promise.all(this.shadowRoot.getAnimations().map((e2) => e2.finished)).then(() => {
      t.signal.aborted || (this.dispatchEvent(new Event("animationsfinish")), this._abortAnimationsFinish = void 0);
    }), this._abortAnimationsFinish = t;
  }
};
D.defaultProps = {
  transformTiming: {
    duration: 900,
    // Make sure to keep this minified:
    easing: "linear(0,.005,.019,.039,.066,.096,.129,.165,.202,.24,.278,.316,.354,.39,.426,.461,.494,.526,.557,.586,.614,.64,.665,.689,.711,.731,.751,.769,.786,.802,.817,.831,.844,.856,.867,.877,.887,.896,.904,.912,.919,.925,.931,.937,.942,.947,.951,.955,.959,.962,.965,.968,.971,.973,.976,.978,.98,.981,.983,.984,.986,.987,.988,.989,.99,.991,.992,.992,.993,.994,.994,.995,.995,.996,.996,.9963,.9967,.9969,.9972,.9975,.9977,.9979,.9981,.9982,.9984,.9985,.9987,.9988,.9989,1)"
  },
  spinTiming: void 0,
  opacityTiming: { duration: 450, easing: "ease-out" },
  animated: true,
  trend: (n2, t) => Math.sign(t - n2),
  respectMotionPreference: true,
  plugins: void 0,
  digits: void 0
};
var F = class {
  constructor(t, e2, i3, { className: s3, ...o } = {}) {
    this.flow = t, this._integer = new b2(t, e2, {
      justify: "right",
      part: "integer"
    }), this._fraction = new b2(t, i3, {
      justify: "left",
      part: "fraction"
    }), this._inner = p2("span", {
      className: "number__inner"
    }, [this._integer.el, this._fraction.el]), this.el = p2("span", {
      ...o,
      part: "number",
      className: `number ${s3 ?? ""}`
    }, [this._inner]);
  }
  willUpdate() {
    this._prevWidth = this.el.offsetWidth, this._prevLeft = this.el.getBoundingClientRect().left, this._integer.willUpdate(), this._fraction.willUpdate();
  }
  update({ integer: t, fraction: e2 }) {
    this._integer.update(t), this._fraction.update(e2);
  }
  didUpdate() {
    const t = this.el.getBoundingClientRect();
    this._integer.didUpdate(), this._fraction.didUpdate();
    const e2 = this._prevLeft - t.left, i3 = this.el.offsetWidth, s3 = this._prevWidth - i3;
    this.el.style.setProperty("--width", String(i3)), this.el.animate({
      [c]: [`${e2}px`, "0px"],
      [u]: [s3, 0]
    }, {
      ...this.flow.transformTiming,
      composite: "accumulate"
    });
  }
};
var E = class {
  constructor(t, e2, { justify: i3, className: s3, ...o }, a) {
    this.flow = t, this.children = /* @__PURE__ */ new Map(), this.onCharRemove = (d2) => () => {
      this.children.delete(d2);
    }, this.justify = i3;
    const r2 = e2.map((d2) => this.addChar(d2).el);
    this.el = p2("span", {
      ...o,
      className: `section section--justify-${i3} ${s3 ?? ""}`
    }, a ? a(r2) : r2);
  }
  addChar(t, { startDigitsAtZero: e2 = false, ...i3 } = {}) {
    const s3 = t.type === "integer" || t.type === "fraction" ? new x2(this, t.type, e2 ? 0 : t.value, t.pos, {
      ...i3,
      onRemove: this.onCharRemove(t.key)
    }) : new I(this, t.type, t.value, {
      ...i3,
      onRemove: this.onCharRemove(t.key)
    });
    return this.children.set(t.key, s3), s3;
  }
  unpop(t) {
    t.el.removeAttribute("inert"), t.el.style.top = "", t.el.style[this.justify] = "";
  }
  pop(t) {
    t.forEach((e2) => {
      e2.el.style.top = `${e2.el.offsetTop}px`, e2.el.style[this.justify] = `${V2(e2.el, this.justify)}px`;
    }), t.forEach((e2) => {
      e2.el.setAttribute("inert", ""), e2.present = false;
    });
  }
  addNewAndUpdateExisting(t) {
    const e2 = /* @__PURE__ */ new Map(), i3 = /* @__PURE__ */ new Map(), s3 = this.justify === "left", o = s3 ? "prepend" : "append";
    if (k2(t, (a) => {
      let r2;
      this.children.has(a.key) ? (r2 = this.children.get(a.key), i3.set(a, r2), this.unpop(r2), r2.present = true) : (r2 = this.addChar(a, { startDigitsAtZero: true, animateIn: true }), e2.set(a, r2)), this.el[o](r2.el);
    }, { reverse: s3 }), this.flow.computedAnimated) {
      const a = this.el.getBoundingClientRect();
      e2.forEach((r2) => {
        r2.willUpdate(a);
      });
    }
    e2.forEach((a, r2) => {
      a.update(r2.value);
    }), i3.forEach((a, r2) => {
      a.update(r2.value);
    });
  }
  willUpdate() {
    const t = this.el.getBoundingClientRect();
    this._prevOffset = t[this.justify], this.children.forEach((e2) => e2.willUpdate(t));
  }
  didUpdate() {
    const t = this.el.getBoundingClientRect();
    this.children.forEach((s3) => s3.didUpdate(t));
    const e2 = t[this.justify], i3 = this._prevOffset - e2;
    i3 && this.children.size && this.el.animate({
      transform: [`translateX(${i3}px)`, "none"]
    }, {
      ...this.flow.transformTiming,
      composite: "accumulate"
    });
  }
};
var b2 = class extends E {
  update(t) {
    const e2 = /* @__PURE__ */ new Map();
    this.children.forEach((i3, s3) => {
      t.find((o) => o.key === s3) || e2.set(s3, i3), this.unpop(i3);
    }), this.addNewAndUpdateExisting(t), e2.forEach((i3) => {
      i3 instanceof x2 && i3.update(0);
    }), this.pop(e2);
  }
};
var A = class extends E {
  update(t) {
    const e2 = /* @__PURE__ */ new Map();
    this.children.forEach((i3, s3) => {
      t.find((o) => o.key === s3) || e2.set(s3, i3);
    }), this.pop(e2), this.addNewAndUpdateExisting(t);
  }
};
var y2 = class {
  constructor(t, e2, { onRemove: i3, animateIn: s3 = false } = {}) {
    this.flow = t, this.el = e2, this._present = true, this._remove = () => {
      var o;
      this.el.remove(), (o = this._onRemove) == null || o.call(this);
    }, this.el.classList.add("animate-presence"), this.flow.computedAnimated && s3 && this.el.animate({
      [g]: [-0.9999, 0]
    }, {
      ...this.flow.opacityTiming,
      composite: "accumulate"
    }), this._onRemove = i3;
  }
  get present() {
    return this._present;
  }
  set present(t) {
    if (this._present !== t) {
      if (this._present = t, t ? this.el.removeAttribute("inert") : this.el.setAttribute("inert", ""), !this.flow.computedAnimated) {
        t || this._remove();
        return;
      }
      this.el.style.setProperty("--_number-flow-d-opacity", t ? "0" : "-.999"), this.el.animate({
        [g]: t ? [-0.9999, 0] : [0.999, 0]
      }, {
        ...this.flow.opacityTiming,
        composite: "accumulate"
      }), t ? this.flow.removeEventListener("animationsfinish", this._remove) : this.flow.addEventListener("animationsfinish", this._remove, {
        once: true
      });
    }
  }
};
var R = class extends y2 {
  constructor(t, e2, i3, s3) {
    super(t.flow, i3, s3), this.section = t, this.value = e2, this.el = i3;
  }
};
var x2 = class extends R {
  constructor(t, e2, i3, s3, o) {
    var c3, f3;
    const a = (((f3 = (c3 = t.flow.digits) == null ? void 0 : c3[s3]) == null ? void 0 : f3.max) ?? 9) + 1, r2 = Array.from({ length: a }).map((u2, m2) => {
      const g3 = p2("span", { className: "digit__num" }, [
        document.createTextNode(String(m2))
      ]);
      return m2 !== i3 && g3.setAttribute("inert", ""), g3.style.setProperty("--n", String(m2)), g3;
    }), d2 = p2("span", {
      part: `digit ${e2}-digit`,
      className: "digit"
    }, r2);
    d2.style.setProperty("--current", String(i3)), d2.style.setProperty("--length", String(a)), super(t, i3, d2, o), this.pos = s3, this._onAnimationsFinish = () => {
      this.el.classList.remove("is-spinning");
    }, this._numbers = r2, this.length = a;
  }
  willUpdate(t) {
    const e2 = this.el.getBoundingClientRect();
    this._prevValue = this.value;
    const i3 = e2[this.section.justify] - t[this.section.justify], s3 = e2.width / 2;
    this._prevCenter = this.section.justify === "left" ? i3 + s3 : i3 - s3;
  }
  update(t) {
    this.el.style.setProperty("--current", String(t)), this._numbers.forEach((e2, i3) => i3 === t ? e2.removeAttribute("inert") : e2.setAttribute("inert", "")), this.value = t;
  }
  didUpdate(t) {
    const e2 = this.el.getBoundingClientRect(), i3 = e2[this.section.justify] - t[this.section.justify], s3 = e2.width / 2, o = this.section.justify === "left" ? i3 + s3 : i3 - s3, a = this._prevCenter - o;
    a && this.el.animate({
      transform: [`translateX(${a}px)`, "none"]
    }, {
      ...this.flow.transformTiming,
      composite: "accumulate"
    });
    const r2 = this.getDelta();
    r2 && (this.el.classList.add("is-spinning"), this.el.animate({
      [h]: [-r2, 0]
    }, {
      ...this.flow.spinTiming ?? this.flow.transformTiming,
      composite: "accumulate"
    }), this.flow.addEventListener("animationsfinish", this._onAnimationsFinish, { once: true }));
  }
  getDelta() {
    var i3;
    if (this.flow.plugins)
      for (const s3 of this.flow.plugins) {
        const o = (i3 = s3.getDelta) == null ? void 0 : i3.call(s3, this.value, this._prevValue, this);
        if (o != null)
          return o;
      }
    const t = this.value - this._prevValue, e2 = this.flow.computedTrend || Math.sign(t);
    return e2 < 0 && this.value > this._prevValue ? this.value - this.length - this._prevValue : e2 > 0 && this.value < this._prevValue ? this.length - this._prevValue + this.value : t;
  }
};
var I = class extends R {
  constructor(t, e2, i3, s3) {
    const o = p2("span", {
      className: "symbol__value",
      textContent: i3
    });
    super(t, i3, p2("span", {
      part: `symbol ${e2}`,
      className: "symbol"
    }, [o]), s3), this.type = e2, this._children = /* @__PURE__ */ new Map(), this._onChildRemove = (a) => () => {
      this._children.delete(a);
    }, this._children.set(i3, new y2(this.flow, o, {
      onRemove: this._onChildRemove(i3)
    }));
  }
  willUpdate(t) {
    if (this.type === "decimal")
      return;
    const e2 = this.el.getBoundingClientRect();
    this._prevOffset = e2[this.section.justify] - t[this.section.justify];
  }
  update(t) {
    if (this.value !== t) {
      const e2 = this._children.get(this.value);
      e2 && (e2.present = false);
      const i3 = this._children.get(t);
      if (i3)
        i3.present = true;
      else {
        const s3 = p2("span", {
          className: "symbol__value",
          textContent: t
        });
        this.el.appendChild(s3), this._children.set(t, new y2(this.flow, s3, {
          animateIn: true,
          onRemove: this._onChildRemove(t)
        }));
      }
    }
    this.value = t;
  }
  didUpdate(t) {
    if (this.type === "decimal")
      return;
    const i3 = this.el.getBoundingClientRect()[this.section.justify] - t[this.section.justify], s3 = this._prevOffset - i3;
    s3 && this.el.animate({
      transform: [`translateX(${s3}px)`, "none"]
    }, { ...this.flow.transformTiming, composite: "accumulate" });
  }
};

// node_modules/@number-flow/vue/dist/index.js
var k3 = String.raw;
true_default && (() => {
  try {
    document.createElement("div").animate({ opacity: 0 }, { easing: "linear(0, 1)" });
  } catch {
    return false;
  }
  return true;
})();
true_default && typeof CSS < "u" && CSS.supports && CSS.supports("line-height", "mod(1,1)");
true_default && typeof matchMedia < "u" && matchMedia("(prefers-reduced-motion: reduce)");
var _2 = "--_number-flow-d-opacity";
var C2 = "--_number-flow-d-width";
var w2 = "--_number-flow-dx";
var E2 = "--_number-flow-d";
(() => {
  try {
    return CSS.registerProperty({
      name: _2,
      syntax: "<number>",
      inherits: false,
      initialValue: "0"
    }), CSS.registerProperty({
      name: w2,
      syntax: "<length>",
      inherits: true,
      initialValue: "0px"
    }), CSS.registerProperty({
      name: C2,
      syntax: "<number>",
      inherits: false,
      initialValue: "0"
    }), CSS.registerProperty({
      name: E2,
      syntax: "<number>",
      inherits: true,
      initialValue: "0"
    }), true;
  } catch {
    return false;
  }
})();
var S2 = "var(--number-flow-char-height, 1em)";
var g2 = "round(nearest, calc(var(--number-flow-mask-height, 0.25em) / 2), 1px)";
var s2 = `calc(${g2} * 2)`;
var $2 = "var(--number-flow-mask-width, 0.5em)";
var c2 = `calc(${$2} / var(--scale-x))`;
var f2 = "#000 0, transparent 71%";
var D2 = k3`:host{display:inline-block;direction:ltr;white-space:nowrap;isolation:isolate;line-height:${S2} !important}.number,.number__inner{display:inline-block;transform-origin:left top}:host([data-will-change]) :is(.number,.number__inner,.section,.digit,.digit__num,.symbol){will-change:transform}.number{--scale-x:calc(1 + var(${C2}) / var(--width));transform:translateX(var(${w2})) scaleX(var(--scale-x));margin:0 calc(-1 * ${$2});position:relative;-webkit-mask-image:linear-gradient(to right,transparent 0,#000 ${c2},#000 calc(100% - ${c2}),transparent ),linear-gradient(to bottom,transparent 0,#000 ${s2},#000 calc(100% - ${s2}),transparent 100% ),radial-gradient(at bottom right,${f2}),radial-gradient(at bottom left,${f2}),radial-gradient(at top left,${f2}),radial-gradient(at top right,${f2});-webkit-mask-size:100% calc(100% - ${s2} * 2),calc(100% - ${c2} * 2) 100%,${c2} ${s2},${c2} ${s2},${c2} ${s2},${c2} ${s2};-webkit-mask-position:center,center,top left,top right,bottom right,bottom left;-webkit-mask-repeat:no-repeat}.number__inner{padding:${g2} ${$2};transform:scaleX(calc(1 / var(--scale-x))) translateX(calc(-1 * var(${w2})))}:host > :not(.number){z-index:5}.section,.symbol{display:inline-block;position:relative;isolation:isolate}.section::after{content:'\200b';display:inline-block}.section--justify-left{transform-origin:center left}.section--justify-right{transform-origin:center right}.section > [inert],.symbol > [inert]{margin:0 !important;position:absolute !important;z-index:-1}.digit{display:inline-block;position:relative;--c:var(--current) + var(${E2})}.digit__num,.number .section::after{padding:${g2} 0}.digit__num{display:inline-block;--offset-raw:mod(var(--length) + var(--n) - mod(var(--c),var(--length)),var(--length));--offset:calc( var(--offset-raw) - var(--length) * round(down,var(--offset-raw) / (var(--length) / 2),1) );--y:clamp(-100%,var(--offset) * 100%,100%);transform:translateY(var(--y))}.digit__num[inert]{position:absolute;top:0;left:50%;transform:translateX(-50%) translateY(var(--y))}.digit:not(.is-spinning) .digit__num[inert]{display:none}.symbol__value{display:inline-block;mix-blend-mode:plus-lighter;white-space:pre}.section--justify-left .symbol > [inert]{left:0}.section--justify-right .symbol > [inert]{right:0}.animate-presence{opacity:calc(1 + var(${_2}))}`;
var O = k3`:host{display:inline-block;direction:ltr;white-space:nowrap;line-height:${S2} !important}span{display:inline-block}:host([data-will-change]) span{will-change:transform}.number,.digit{padding:${g2} 0}.symbol{white-space:pre}`;
var q = (e2 = "") => k3`number-flow${e2} > span{font-kerning:none;display:inline-block;line-height:${S2} !important;padding:${s2} 0}`;
var J = (e2) => [O, q(e2), D2];
var B2 = Symbol();
var te = defineComponent({
  __name: "NumberFlowGroup",
  setup(e2) {
    const l2 = /* @__PURE__ */ new Set();
    let a = false;
    return provide(B2, (i3, m2) => {
      l2.add(i3), watch(
        m2,
        async () => {
          a || (a = true, l2.forEach(async (t) => {
            var d2;
            !t.value || !t.value.created || (t.value.willUpdate(), await nextTick(), (d2 = t.value) == null || d2.didUpdate());
          }), await nextTick(), a = false);
        }
        // { flush: 'pre' } // default
      ), onUnmounted(() => {
        l2.delete(i3);
      });
    }), (i3, m2) => renderSlot(i3.$slots, "default");
  }
});
var K = ["batched", "trend", "plugins", "animated", "transformTiming", "spinTiming", "opacityTiming", "respectMotionPreference", "nonce", "data-will-change", "digits", "innerHTML", "data"];
var ne = defineComponent({
  inheritAttrs: false,
  __name: "index",
  props: {
    transformTiming: { default: () => D.defaultProps.transformTiming },
    spinTiming: { default: () => D.defaultProps.spinTiming },
    opacityTiming: { default: () => D.defaultProps.opacityTiming },
    animated: { type: Boolean, default: () => D.defaultProps.animated },
    respectMotionPreference: { type: Boolean, default: () => D.defaultProps.respectMotionPreference },
    trend: { type: [Number, Function], default: () => D.defaultProps.trend },
    plugins: { default: () => D.defaultProps.plugins },
    digits: { default: () => D.defaultProps.digits },
    locales: {},
    format: {},
    value: {},
    prefix: {},
    suffix: {},
    nonce: {},
    willChange: { type: Boolean, default: false }
  },
  emits: ["animationsstart", "animationsfinish"],
  setup(e2, { expose: l2, emit: a }) {
    const r2 = ref();
    l2({ el: r2 });
    const i3 = a, m2 = computed(() => new Intl.NumberFormat(e2.locales, e2.format)), t = computed(() => z(e2.value, m2.value, e2.prefix, e2.suffix)), d2 = true_default ? void 0 : C(t.value, { nonce: e2.nonce, elementSuffix: "-vue" }), u2 = inject(B2, void 0);
    return u2 == null || u2(r2, t), (n2, p3) => (openBlock(), createElementBlock("number-flow-vue", mergeProps({
      ref_key: "el",
      ref: r2
    }, n2.$attrs, {
      batched: !!unref(u2),
      trend: n2.trend,
      plugins: n2.plugins,
      animated: n2.animated,
      transformTiming: n2.transformTiming,
      spinTiming: n2.spinTiming,
      opacityTiming: n2.opacityTiming,
      respectMotionPreference: n2.respectMotionPreference,
      nonce: n2.nonce,
      "data-will-change": n2.willChange ? "" : void 0,
      digits: n2.digits,
      innerHTML: unref(d2),
      "data-allow-mismatch": "",
      onAnimationsstart: p3[0] || (p3[0] = (A2) => i3("animationsstart")),
      onAnimationsfinish: p3[1] || (p3[1] = (A2) => i3("animationsfinish")),
      data: t.value
    }), null, 16, K));
  }
});
var ie = J("-vue");
X("number-flow-vue", D);
function ae({
  respectMotionPreference: e2 = true
} = {}) {
  const l2 = ref(false), a = ref(false);
  return onMounted(() => {
    var r2;
    l2.value = B, a.value = ((r2 = _) == null ? void 0 : r2.matches) ?? false;
  }), watchEffect((r2) => {
    var m2;
    if (!toValue(e2))
      return;
    const i3 = ({ matches: t }) => {
      a.value = t;
    };
    (m2 = _) == null || m2.addEventListener("change", i3), r2(() => {
      var t;
      (t = _) == null || t.removeEventListener("change", i3);
    });
  }), computed(
    () => l2.value && (!toValue(e2) || !a.value)
  );
}
export {
  D as NumberFlowElement,
  te as NumberFlowGroup,
  l as continuous,
  ne as default,
  ie as styles,
  ae as useCanAnimate
};
//# sourceMappingURL=@number-flow_vue.js.map
